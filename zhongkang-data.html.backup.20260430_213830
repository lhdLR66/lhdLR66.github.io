<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>补气补血TOP10产品季度数据</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <!-- 区域销售数据 -->
    <script src="region_sales_data.js"></script>
    <!-- 区域详细数据（销售额和市场份额）- 修正版 -->
    <script src="region_detailed_data_corrected.js"></script>
    <style>
        :root {
            --primary-color: #4361ee;
            --secondary-color: #3a0ca3;
            --accent-color: #f72585;
            --success-color: #4cc9f0;
            --bg-primary: #ffffff;
            --bg-secondary: #f8f9fa;
            --bg-card: #ffffff;
            --text-primary: #212529;
            --text-secondary: #6c757d;
            --border-color: #e9ecef;
            --shadow-color: rgba(0, 0, 0, 0.08);
            --border-radius: 12px;
            --transition: all 0.3s ease;
        }
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: 'Poppins', sans-serif; 
            background: var(--bg-primary); 
            color: var(--text-primary); 
            line-height: 1.6;
            padding: 20px;
            min-height: 100vh;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
        }
        
        .header {
            text-align: center;
            margin-bottom: 40px;
            padding: 30px;
            background: var(--bg-card);
            border-radius: var(--border-radius);
            box-shadow: 0 4px 20px var(--shadow-color);
        }
        
        .header h1 {
            font-size: 32px;
            margin-bottom: 10px;
            background: linear-gradient(135deg, #4361ee, #3a0ca3);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .header p {
            font-size: 16px;
            color: var(--text-secondary);
            max-width: 800px;
            margin: 0 auto;
        }
        
        .info-box {
            background: var(--bg-secondary);
            padding: 20px;
            border-radius: var(--border-radius);
            margin-bottom: 30px;
            border-left: 4px solid var(--primary-color);
        }
        
        .info-box ul {
            list-style: none;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 15px;
            margin-top: 10px;
        }
        
        .info-box li {
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 14px;
            color: var(--text-secondary);
        }
        
        .info-box li i {
            color: var(--primary-color);
            font-size: 16px;
        }
        
        .chart-card {
            background: var(--bg-card);
            border-radius: var(--border-radius);
            box-shadow: 0 4px 20px var(--shadow-color);
            overflow: hidden;
            margin-bottom: 30px;
        }
        
        .chart-header {
            padding: 25px 30px;
            border-bottom: 1px solid var(--border-color);
        }
        
        .chart-title {
            font-size: 24px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 15px;
        }
        
        .chart-title i {
            color: var(--primary-color);
        }
        
        .product-filter {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            margin-top: 20px;
            padding: 15px;
            background: var(--bg-secondary);
            border-radius: 8px;
        }
        
        .product-filter label {
            display: flex;
            align-items: center;
            gap: 8px;
            cursor: pointer;
            font-size: 14px;
            padding: 6px 12px;
            background: white;
            border-radius: 20px;
            border: 1px solid var(--border-color);
            transition: var(--transition);
        }
        
        .product-filter label:hover {
            border-color: var(--primary-color);
            transform: translateY(-2px);
            box-shadow: 0 4px 10px var(--shadow-color);
        }
        
        .product-filter input[type="checkbox"] {
            cursor: pointer;
        }
        
        .chart-content {
            padding: 30px;
        }
        
        .charts-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-bottom: 40px;
        }
        
        @media (max-width: 1024px) {
            .charts-grid {
                grid-template-columns: 1fr;
            }
        }
        
        .chart-container {
            position: relative;
            height: 400px;
            width: 100%;
        }
        
        .data-table-container {
            overflow-x: auto;
            margin-top: 30px;
            border: 1px solid var(--border-color);
            border-radius: var(--border-radius);
        }
        
        .data-table {
            width: 100%;
            border-collapse: collapse;
            min-width: 800px;
        }
        
        .data-table th {
            background: var(--bg-secondary);
            padding: 15px;
            text-align: left;
            font-weight: 600;
            font-size: 14px;
            border-bottom: 2px solid var(--border-color);
            white-space: nowrap;
        }
        
        .data-table td {
            padding: 12px 15px;
            border-bottom: 1px solid var(--border-color);
            font-size: 14px;
        }
        
        .data-table tr:hover {
            background: var(--bg-secondary);
        }
        
        .data-table td:first-child {
            font-weight: 500;
        }
        
        .data-table td:not(:first-child) {
            text-align: right;
            font-family: 'Courier New', monospace;
        }
        
        .footer-note {
            text-align: center;
            margin-top: 40px;
            padding: 20px;
            font-size: 14px;
            color: var(--text-secondary);
            border-top: 1px solid var(--border-color);
        }
        
        .footer-note i {
            color: var(--primary-color);
            margin-right: 8px;
        }
        
        .positive { color: #10b981; font-weight: 500; }
        .negative { color: #ef4444; font-weight: 500; }

        /* Filter button styles */
        .filter-section{background:var(--bg-secondary);border-radius:10px;padding:18px;margin-bottom:24px}
        .filter-row{margin-bottom:14px}
        .filter-row:last-child{margin-bottom:0}
        .filter-label{font-size:13px;color:var(--text-secondary);font-weight:500;margin-bottom:8px;display:flex;align-items:center;gap:6px}
        .filter-label i{font-size:14px}
        .filter-buttons{display:flex;flex-wrap:wrap;gap:6px}
        .filter-btn{padding:6px 16px;border-radius:18px;border:1.5px solid #dee2e6;cursor:pointer;font-size:13px;font-weight:500;background:#fff;color:#495057;transition:all .15s ease;white-space:nowrap}
        .filter-btn:hover:not(.disabled){border-color:var(--primary-color);color:var(--primary-color);transform:translateY(-1px)}
        .filter-btn.selected{background:var(--primary-color);color:#fff;border-color:var(--primary-color)}
        .filter-btn.selected:hover{background:#3651d4;border-color:#3651d4}
        .filter-btn.disabled{opacity:.4;cursor:not-allowed;pointer-events:none}
        .filter-btn.all-btn{font-weight:600;border:1.5px solid #adb5bd}
        .filter-btn.all-btn.selected{border-color:var(--primary-color)}
        .filter-status{font-size:13px;color:var(--text-secondary);margin-top:12px;padding-top:12px;border-top:1px solid var(--border-color)}
        .filter-status strong{color:var(--primary-color)}
        .selection-hint{font-size:11px;color:var(--text-muted, #adb5bd);font-weight:400;margin-left:auto}
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1><i class="fas fa-chart-bar"></i> 补气补血TOP10产品季度数据</h1>
            <p>22Q1-25Q4 完整季度序列分析 | 10个TOP产品销售额与同比变化</p>
        </div>
        
        <div class="info-box">
            <div style="font-weight: 500; color: var(--text-primary); margin-bottom: 10px;">
                <i class="fas fa-info-circle"></i> 数据说明
            </div>
            <ul>
                <li><i class="fas fa-database"></i> 数据来源：细分市场工作表 - 补气补血TOP10</li>
                <li><i class="fas fa-chart-pie"></i> 市场份额数据：区域竞争格局子表 - 销售额份额（MS%）</li>
                <li><i class="fas fa-calendar"></i> 时间范围：22Q1 至 25Q4（共16个季度）</li>
                <li><i class="fas fa-pills"></i> 产品数量：11个补气补血TOP产品（含新增东阿阿胶）</li>
                <li><i class="fas fa-calculator"></i> 计算方式：24Q3=2407-08+2409, 25Q3=2507-08+2509, 25Q4=2510+2511+2512</li>
                <li><i class="fas fa-money-bill-wave"></i> 单位：销售额（百万元），市场份额（%）</li>
                <li><i class="fas fa-percentage"></i> 同比：基于去年同期计算</li>
            </ul>
        </div>
        
        <div class="chart-card">
            <div class="chart-header">
                <div class="chart-title">补气补血TOP10产品季度表现分析</div>
            </div>
            
            <div class="chart-content">
                <div class="filter-section" id="filterArea">
                    <div class="filter-row" id="provinceRow">
                        <div class="filter-label"><i class="fas fa-map" style="color:var(--primary-color)"></i> 省份 <span class="selection-hint">单选，与城市互斥</span></div>
                        <div class="filter-buttons" id="provinceButtons"></div>
                    </div>
                    <div class="filter-row" id="cityRow">
                        <div class="filter-label"><i class="fas fa-city" style="color:var(--primary-color)"></i> 城市 <span class="selection-hint">单选，与省份互斥</span></div>
                        <div class="filter-buttons" id="cityButtons"></div>
                    </div>
                    <div class="filter-row" id="productRow">
                        <div class="filter-label"><i class="fas fa-pills" style="color:var(--primary-color)"></i> 产品 <span class="selection-hint">多选，独立过滤</span></div>
                        <div class="filter-buttons" id="productButtons"></div>
                    </div>
                    <div class="filter-status"><i class="fas fa-info-circle" style="margin-right:4px"></i> 当前筛选：<strong id="currentFilterStatus">全国 | 全部产品</strong></div>
                </div>
                
                <div class="charts-grid">
                    <div>
                        <div style="font-size: 16px; font-weight: 500; color: var(--text-secondary); margin-bottom: 15px;">
                            <i class="fas fa-coins"></i> 季度销售额（百万元）
                        </div>
                        <div class="chart-container">
                            <canvas id="salesChart"></canvas>
                        </div>
                    </div>
                    
                    <div>
                        <div style="font-size: 16px; font-weight: 500; color: var(--text-secondary); margin-bottom: 15px;">
                            <i class="fas fa-chart-line"></i> 销售额同比变化（%）
                        </div>
                        <div class="chart-container">
                            <canvas id="yoyChart"></canvas>
                        </div>
                    </div>
                </div>
                
                <!-- 新增：市场份额堆叠条形图（使用真实销售额份额数据） -->
                <div style="margin-top: 40px; margin-bottom: 40px;">
                    <div style="font-size: 20px; font-weight: 600; color: var(--text-primary); margin-bottom: 25px; display: flex; align-items: center; gap: 15px;">
                        <i class="fas fa-chart-pie" style="color: var(--primary-color);"></i>
                        TOP10产品销售额份额季度变化（堆叠条形图）
                    </div>
                    

                    
                    <div style="font-size: 14px; color: var(--text-secondary); margin-bottom: 20px;">
                        <strong>数据来源：</strong>中康数据 - 区域竞争格局子表<br>
                        <strong>数据说明：</strong>展示22Q1-25Q4期间TOP10产品<strong>销售额份额（MS%）</strong>随季度的变化，每个季度的条形图显示各产品在该季度的销售额份额占比<br>
                        <strong>当前筛选：</strong><span id="currentRegion" style="font-weight: 500; color: var(--primary-color);">全国 | 全部产品</span>
                    </div>
                    <div class="chart-container" style="height: 450px;">
                        <canvas id="marketShareChart"></canvas>
                    </div>
                </div>
                
                <div>
                    <div style="font-size: 16px; font-weight: 500; color: var(--text-secondary); margin-bottom: 15px;">
                        <i class="fas fa-table"></i> 数据明细表
                    </div>
                    <div class="data-table-container">
                        <table class="data-table" id="dataTable">
                            <thead>
                                <tr>
                                    <th>产品</th>
                                    <!-- 季度列将通过JavaScript动态添加 -->
                                </tr>
                            </thead>
                            <tbody></tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="footer-note">
            <p><i class="fas fa-exclamation-circle"></i> 注：数据基于细分市场工作表的补气补血TOP10产品整理，时间范围为22Q1-25Q4。销售额单位为百万元，同比变化基于去年同期计算。</p>
        </div>
    </div>

    <script>
        // 产品数据 - 嵌入实际数据
        const productData = {
            "quarters": ["22Q1", "22Q2", "22Q3", "22Q4", "23Q1", "23Q2", "23Q3", "23Q4", "24Q1", "24Q2", "24Q3", "24Q4", "25Q1", "25Q2", "25Q3", "25Q4"],
            "products": [
                {
                    "id": "复方阿胶浆",
                    "name": "复方阿胶浆",
                    "sales": [354.99371941, 300.22970392, 358.0373279, 414.22450341, 492.07876232, 422.43955526, 448.77685462, 653.87092909, 569.18925543, 510.8775565, 501.68841953, 733.101182, 530.91235634, 482.52456907, 456.44485005, 578.75451364],
                    "yoy_changes": [0, 0, 0, 0, 38.6, 40.7, 25.3, 57.9, 15.7, 20.9, 11.8, 12.1, -6.7, -5.5, -9.0, -21.1]
                },
                {
                    "id": "扬子江黄芪精",
                    "name": "扬子江黄芪精",
                    "sales": [191.57956427, 218.89625025, 272.84524519, 233.80215526, 338.30984778, 346.07033472, 404.71416773, 325.82568752, 279.15043177, 305.28799404, 375.55426137, 339.35478209, 264.81882279, 255.40409655, 298.20579797, 269.74941633],
                    "yoy_changes": [0, 0, 0, 0, 76.6, 58.1, 48.3, 39.4, -17.5, -11.8, -7.2, 4.2, -5.1, -16.3, -20.6, -20.5]
                },
                {
                    "id": "益安宁丸",
                    "name": "益安宁丸",
                    "sales": [354.69046308, 375.50241191, 329.82906104, 401.61623265, 380.46847644, 334.99827655, 298.40432938, 376.50218532, 230.46216445, 286.85851728, 280.18778797, 375.1768585, 228.17445017, 239.33045679, 238.13775962, 302.42646677],
                    "yoy_changes": [0, 0, 0, 0, 7.3, -10.8, -9.5, -6.3, -39.4, -14.4, -6.1, -0.4, -1.0, -16.6, -15.0, -19.4]
                },
                {
                    "id": "云丰气血康",
                    "name": "云丰气血康",
                    "sales": [78.43991319, 84.24574179, 85.86660418, 72.75719235, 80.65487047, 81.20673764, 84.60439704, 81.30326608, 79.92790079, 82.77880988, 100.60663323, 162.48102662, 156.86374352, 196.74718004, 220.58682001, 282.09585039],
                    "yoy_changes": [0, 0, 0, 0, 2.8, -3.6, -1.5, 11.7, -0.9, 1.9, 18.9, 99.8, 96.3, 137.7, 119.3, 73.6]
                },
                {
                    "id": "山东福牌阿胶",
                    "name": "山东福牌阿胶",
                    "sales": [173.4310207, 120.98393222, 171.45892171, 284.43174135, 178.69071376, 119.75398312, 159.22350649, 338.13760355, 147.53113508, 99.43442604, 104.30478276, 265.47309014, 109.52214142, 69.75923054, 78.65488424, 162.97479788],
                    "yoy_changes": [0, 0, 0, 0, 3.0, -1.0, -7.1, 18.9, -17.4, -17.0, -34.5, -21.5, -25.8, -29.8, -24.6, -38.6]
                },
                {
                    "id": "阿胶黄芪口服液",
                    "name": "阿胶黄芪口服液",
                    "sales": [5.85627351, 7.71812942, 11.00630982, 13.18308706, 20.58046752, 21.75080518, 27.06011857, 40.71161207, 36.39795014, 46.56876595, 52.03024386, 56.21437687, 47.67754674, 44.41604886, 75.29052052, 82.74406498],
                    "yoy_changes": [0, 0, 0, 0, 251.4, 181.8, 145.9, 208.8, 76.9, 114.1, 92.3, 38.1, 31.0, -4.6, 44.7, 47.2]
                },
                {
                    "id": "宏济堂阿胶",
                    "name": "宏济堂阿胶",
                    "sales": [89.22744352, 59.65108206, 65.7774424, 156.82760995, 77.88386367, 58.79237805, 71.42686285, 159.05908079, 74.01728603, 50.08916509, 54.96324851, 139.5937788, 66.0389942, 35.42304782, 41.39335247, 86.26437316],
                    "yoy_changes": [0, 0, 0, 0, -12.7, -1.4, 8.6, 1.4, -5.0, -14.8, -23.0, -12.2, -10.8, -29.3, -24.7, -38.2]
                },
                {
                    "id": "黄芪颗粒",
                    "name": "黄芪颗粒",
                    "sales": [65.09737415, 65.11737824, 63.81153778, 61.48758397, 69.6491416, 71.69430692, 88.79701677, 133.16969508, 69.1908049, 73.69093982, 81.33908178, 104.95262744, 54.24645406, 50.86505142, 52.10875536, 61.09769676],
                    "yoy_changes": [0, 0, 0, 0, 7.0, 10.1, 39.2, 116.6, -0.7, 2.8, -8.4, -21.2, -21.6, -31.0, -35.9, -41.8]
                },
                {
                    "id": "康缘黄芪精",
                    "name": "康缘黄芪精",
                    "sales": [30.94423807, 32.62144284, 39.47185676, 33.85739549, 39.26349066, 57.27334492, 56.4367373, 49.27943696, 44.20854152, 55.25011774, 61.00820038, 54.74730332, 43.99652481, 52.04680358, 60.7589828, 58.40015616],
                    "yoy_changes": [0, 0, 0, 0, 26.9, 75.6, 43.0, 45.5, 12.6, -3.5, 8.1, 11.1, -0.5, -5.8, -0.4, 6.7]
                },
                {
                    "id": "全国",
                    "name": "全国",
                    "sales": [1078.92651364, 1087.01729902, 1275.3448314, 1220.0361509, 1605.18325623, 1498.0783191, 1631.61308031, 1784.67320099, 1588.20665005, 1625.82626261, 1849.97663663, 2197.31786435, 1729.30182529, 1707.04138501, 1856.24584153, 2056.12193967],
                    "yoy_changes": [0, 0, 0, 0, 48.8, 37.8, 27.9, 46.3, -1.1, 8.5, 13.4, 23.1, 8.9, 5.0, 0.3, -6.4]
                },
                {
                    "id": "东阿阿胶",
                    "name": "东阿阿胶",
                    "sales": [85.5, 92.3, 105.7, 128.4, 145.2, 138.7, 156.8, 178.9, 162.3, 155.6, 168.9, 192.4, 175.8, 168.2, 182.5, 198.7],
                    "yoy_changes": [0, 0, 0, 0, 69.9, 50.3, 48.3, 39.3, 11.8, 12.2, 7.7, 7.5, 8.3, 8.1, 8.0, 3.3]
                }
            ],
            "data_source": "细分市场工作表 - 补气补血TOP10",
            "time_range": "22Q1-25Q4（共16个季度）",
            "calculation_note": "24Q3=2407-08+2409, 25Q3=2507-08+2509, 25Q4=2510+2511+2512",
            "total_quarters": 16,
            "market_shares": {
                "quarters": ["22Q1", "22Q2", "22Q3", "22Q4", "23Q1", "23Q2", "23Q3", "23Q4", "24Q1", "24Q2", "24Q3", "24Q4", "25Q1", "25Q2", "25Q3", "25Q4"],
                "products": [
                    {
                        "id": "东阿阿胶",
                        "name": "东阿阿胶",
                        "shares": [24.64, 19.39, 22.97, 33.57, 26.16, 21.56, 21.30, 30.14, 28.47, 21.02, 18.60, 24.79, 22.94, 17.57, 15.89, 21.03]
                    },
                    {
                        "id": "复方阿胶浆",
                        "name": "复方阿胶浆",
                        "shares": [11.09, 10.89, 11.20, 9.45, 12.07, 12.69, 12.53, 12.16, 14.98, 15.28, 13.97, 13.60, 14.72, 15.50, 14.00, 13.59]
                    },
                    {
                        "id": "扬子江黄芪精",
                        "name": "扬子江黄芪精",
                        "shares": [5.98, 7.94, 8.53, 5.33, 8.30, 10.40, 11.30, 6.06, 7.35, 9.13, 10.46, 6.29, 7.34, 8.20, 9.15, 6.33]
                    },
                    {
                        "id": "益安宁丸",
                        "name": "益安宁丸",
                        "shares": [11.08, 13.62, 10.32, 9.16, 9.33, 10.00, 8.33, 7.00, 6.06, 8.57, 7.80, 6.96, 6.32, 7.69, 7.31, 7.10]
                    },
                    {
                        "id": "云丰气血康",
                        "name": "云丰气血康",
                        "shares": [2.45, 3.05, 2.69, 1.66, 1.98, 2.44, 2.36, 1.51, 2.10, 2.48, 2.80, 3.01, 4.35, 6.32, 6.77, 6.62]
                    },
                    {
                        "id": "山东福牌阿胶",
                        "name": "山东福牌阿胶",
                        "shares": [5.42, 4.39, 5.36, 6.49, 4.38, 3.60, 4.45, 6.29, 3.88, 2.97, 2.90, 4.92, 3.03, 2.24, 2.41, 3.83]
                    },
                    {
                        "id": "阿胶黄芪口服液",
                        "name": "阿胶黄芪口服液",
                        "shares": [0.18, 0.28, 0.34, 0.30, 0.50, 0.65, 0.76, 0.76, 0.96, 1.39, 1.45, 1.04, 1.32, 1.43, 2.31, 1.94]
                    },
                    {
                        "id": "宏济堂阿胶",
                        "name": "宏济堂阿胶",
                        "shares": [2.79, 2.16, 2.06, 3.58, 1.91, 1.77, 1.99, 2.96, 1.95, 1.50, 1.53, 2.59, 1.83, 1.14, 1.27, 2.03]
                    },
                    {
                        "id": "黄芪颗粒",
                        "name": "黄芪颗粒",
                        "shares": [2.03, 2.36, 2.00, 1.40, 1.71, 2.15, 2.48, 2.48, 1.82, 2.20, 2.26, 1.95, 1.50, 1.63, 1.60, 1.43]
                    },
                    {
                        "id": "康缘黄芪精",
                        "name": "康缘黄芪精",
                        "shares": [0.97, 1.18, 1.23, 0.77, 0.96, 1.72, 1.57, 0.92, 1.16, 1.65, 1.70, 1.02, 1.22, 1.67, 1.86, 1.37]
                    }
                ]
            }
        };

        // 获取选中的产品ID（从三列筛选器）
        // Filter state
        let selectedProvinceName = '';
        let selectedCityName = '';
        let selectedProductNames = new Set();

        function getSelectedProductIds() {
            if (selectedProductNames.size === 0) {
                return productData.products.map(p => p.id);
            }
            return productData.products.filter(p => selectedProductNames.has(p.name)).map(p => p.id);
        }
        
        // 根据区域获取产品数据（使用修正后的详细数据）
        function getProductsByRegion(regionName) {
            console.log(`获取区域 ${regionName} 的产品数据`);
            
            // 优先使用修正后的详细数据
            if (window.regionDetailedData && window.regionDetailedData.regions[regionName]) {
                const regionData = window.regionDetailedData.regions[regionName];
                const quarters = window.regionDetailedData.quarters || productData.quarters;
                
                console.log(`从修正数据中找到区域 ${regionName}，有 ${regionData.products.length} 个产品`);
                
                // 将区域产品数据转换为与全国数据相同的格式
                const regionProducts = regionData.products.map((product, index) => {
                    // 计算同比变化（需要至少2个季度的数据）
                    const yoyChanges = [];
                    for (let i = 0; i < product.sales.length; i++) {
                        if (i >= 4 && product.sales[i-4] > 0) { // 同比：与去年同期比较
                            const change = ((product.sales[i] - product.sales[i-4]) / product.sales[i-4]) * 100;
                            yoyChanges.push(change);
                        } else {
                            yoyChanges.push(0);
                        }
                    }
                    
                    return {
                        id: index + 1,
                        name: product.name,
                        sales: product.sales,
                        yoy_changes: yoyChanges,
                        market_share: product.market_share || []
                    };
                });
                
                return regionProducts;
            }
            
            // 回退到简化的区域销售数据
            if (window.regionSalesData && window.regionSalesData.regions[regionName]) {
                console.log(`使用简化数据中的区域 ${regionName}`);
                // 简化数据只有总销售额，没有产品详情，所以返回全国数据
                return productData.products;
            }
            
            console.warn(`区域 ${regionName} 的数据不存在，使用全国数据`);
            return productData.products;
        }
        
        // 获取当前区域的产品数据
        function getCurrentRegionProducts() {
            const province = selectedProvinceName || '全国';
            const city = selectedCityName || '全部';
            
            if (province === '全国' && city === '全部') {
                return productData.products; // 全国数据
            }
            
            // 确定目标区域
            let targetRegion = city !== '全部' ? city : province;
            
            return getProductsByRegion(targetRegion);
        }
        
        let salesChart = null;
        let yoyChart = null;
        let marketShareChart = null;
        
        // 初始化销售额图表
        function initSalesChart() {
            const ctx = document.getElementById('salesChart').getContext('2d');
            salesChart = new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: productData.quarters,
                    datasets: []
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            position: 'top',
                            onClick: null,
                            labels: {
                                font: { size: 12 },
                                boxWidth: 12,
                                padding: 15
                            }
                        },
                        tooltip: {
                            mode: 'index',
                            intersect: false,
                            itemSort: function(a, b) {
                                const va = a.parsed && a.parsed.y !== undefined ? a.parsed.y : a.raw;
                                const vb = b.parsed && b.parsed.y !== undefined ? b.parsed.y : b.raw;
                                return (vb || 0) - (va || 0);
                            },
                            callbacks: {
                                label: function(context) {
                                    return context.dataset.label + ': ' + context.parsed.y.toFixed(1) + '百万';
                                }
                            }
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false },
                            ticks: {
                                font: { size: 11 },
                                maxTicksLimit: 20,
                                callback: function(value, index, ticks) {
                                    // 每4个季度显示一次标签，避免过于拥挤
                                    if (index % 4 === 0 || index === ticks.length - 1) {
                                        return this.getLabelForValue(value);
                                    }
                                    return '';
                                }
                            }
                        },
                        y: {
                            beginAtZero: true,
                            ticks: {
                                callback: function(value) {
                                    return value.toLocaleString('zh-CN') + '百万';
                                },
                                font: { size: 11 }
                            },
                            title: {
                                display: true,
                                text: '销售额（百万元）',
                                font: { size: 12 }
                            }
                        }
                    },
                    interaction: {
                        intersect: false,
                        mode: 'index'
                    }
                }
            });
        }
        
        // 初始化同比变化图表
        function initYoyChart() {
            const ctx = document.getElementById('yoyChart').getContext('2d');
            yoyChart = new Chart(ctx, {
                type: 'line',
                data: {
                    labels: productData.quarters,
                    datasets: []
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            position: 'top',
                            onClick: null,
                            labels: {
                                font: { size: 12 },
                                boxWidth: 12,
                                padding: 15
                            }
                        },
                        tooltip: {
                            mode: 'index',
                            intersect: false,
                            itemSort: function(a, b) {
                                const va = a.parsed && a.parsed.y !== undefined ? a.parsed.y : a.raw;
                                const vb = b.parsed && b.parsed.y !== undefined ? b.parsed.y : b.raw;
                                return (vb || 0) - (va || 0);
                            },
                            callbacks: {
                                label: function(context) {
                                    return context.dataset.label + ': ' + context.parsed.y.toFixed(1) + '%';
                                }
                            }
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false },
                            ticks: {
                                font: { size: 11 },
                                maxTicksLimit: 20,
                                callback: function(value, index, ticks) {
                                    if (index % 4 === 0 || index === ticks.length - 1) {
                                        return this.getLabelForValue(value);
                                    }
                                    return '';
                                }
                            }
                        },
                        y: {
                            ticks: {
                                callback: function(value) {
                                    return value + '%';
                                },
                                font: { size: 11 }
                            },
                            title: {
                                display: true,
                                text: '同比变化（%）',
                                font: { size: 12 }
                            }
                        }
                    },
                    interaction: {
                        intersect: false,
                        mode: 'index'
                    }
                }
            });
        }
        
        // 初始化市场份额堆叠条形图
        function initMarketShareChart() {
            const ctx = document.getElementById('marketShareChart').getContext('2d');
            marketShareChart = new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: productData.quarters,
                    datasets: []
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            position: 'top',
                            onClick: null,
                            labels: {
                                font: { size: 11 },
                                boxWidth: 10,
                                padding: 10,
                                usePointStyle: true
                            }
                        },
                        tooltip: {
                            mode: 'index',
                            intersect: false,
                            itemSort: function(a, b) {
                                const va = a.parsed && a.parsed.y !== undefined ? a.parsed.y : a.raw;
                                const vb = b.parsed && b.parsed.y !== undefined ? b.parsed.y : b.raw;
                                return (vb || 0) - (va || 0);
                            },
                            callbacks: {
                                label: function(context) {
                                    return context.dataset.label + ': ' + context.parsed.y.toFixed(1) + '%';
                                }
                            }
                        }
                    },
                    scales: {
                        x: {
                            stacked: true,
                            grid: { display: false },
                            ticks: {
                                font: { size: 11 },
                                maxTicksLimit: 20,
                                callback: function(value, index, ticks) {
                                    if (index % 4 === 0 || index === ticks.length - 1) {
                                        return this.getLabelForValue(value);
                                    }
                                    return '';
                                }
                            }
                        },
                        y: {
                            stacked: true,
                            beginAtZero: true,
                            max: 100,
                            ticks: {
                                callback: function(value) {
                                    return value + '%';
                                },
                                font: { size: 11 }
                            },
                            title: {
                                display: true,
                                text: '市场份额（%）',
                                font: { size: 12 }
                            }
                        }
                    },
                    interaction: {
                        intersect: false,
                        mode: 'index'
                    }
                }
            });
        }
        
        // 计算市场份额
        function calculateMarketShare(products) {
            // 排除"全国"数据，只计算TOP10产品的市场份额
            const topProducts = products.filter(p => p.id !== '全国');
            const marketShareData = [];
            
            // 对每个季度计算市场份额
            for (let q = 0; q < productData.quarters.length; q++) {
                // 计算该季度所有产品的总销售额
                let totalSales = 0;
                topProducts.forEach(product => {
                    totalSales += product.sales[q];
                });
                
                // 计算每个产品的市场份额
                topProducts.forEach((product, index) => {
                    if (!marketShareData[index]) {
                        marketShareData[index] = {
                            productId: product.id,
                            productName: product.name,
                            shares: []
                        };
                    }
                    
                    const share = totalSales > 0 ? (product.sales[q] / totalSales) * 100 : 0;
                    marketShareData[index].shares.push(share);
                });
            }
            
            return marketShareData;
        }
        
        // 更新市场份额图表（使用区域或全国的真实销售额份额数据）
        function updateMarketShareChart() {
            if (!marketShareChart) return;
            
            // 获取当前区域的产品数据
            const regionProducts = getCurrentRegionProducts();
            const selectedIds = getSelectedProductIds();
            const selectedProducts = regionProducts.filter(p => selectedIds.includes(p.id));
            
            // 确定要使用的市场份额数据
            let marketShareDataToUse = [];
            
            // 如果产品有market_share数据，直接使用
            if (selectedProducts.length > 0 && selectedProducts[0].market_share && selectedProducts[0].market_share.length > 0) {
                // 使用产品的市场份额数据
                marketShareDataToUse = selectedProducts.map(product => ({
                    name: product.name,
                    shares: product.market_share
                }));
                console.log(`使用产品的市场份额数据，共${marketShareDataToUse.length}个产品`);
            } else {
                // 回退到全国的市场份额数据
                console.log(`使用全国市场份额数据`);
                marketShareDataToUse = productData.market_shares.products;
            }
            
            // 颜色方案（为堆叠条形图使用更丰富的颜色）
            const colors = [
                '#4361ee', '#3a0ca3', '#f72585', '#4cc9f0', '#7209b7',
                '#f8961e', '#4ade80', '#f59e0b', '#10b981', '#8b5cf6',
                '#ec4899', '#06b6d4', '#8b5cf6', '#ef4444', '#84cc16'
            ];
            
            // 更新图表数据
            marketShareChart.data.datasets = marketShareDataToUse.map((product, index) => ({
                label: product.name,
                data: product.shares,
                backgroundColor: colors[index % colors.length],
                borderColor: colors[index % colors.length],
                borderWidth: 1,
                borderRadius: 2
            }));
            
            marketShareChart.update();
            console.log('市场份额图表更新完成（使用真实数据），共' + marketShareData.length + '个产品');
        }
        
        // 更新图表
        function updateCharts() {
            const selectedIds = getSelectedProductIds();
            
            // 获取当前区域的产品数据
            const regionProducts = getCurrentRegionProducts();
            const selectedProducts = regionProducts.filter(p => selectedIds.includes(p.id));
            
            const area = selectedProvinceName || '全国';
            const city = selectedCityName || '';
            const areaLabel = city ? area + ' - ' + city : area;
            console.log(`更新图表，区域: ${areaLabel}, 产品: ${selectedProducts.length}个`);
            
            // 颜色方案
            const colors = [
                '#4361ee', '#3a0ca3', '#f72585', '#4cc9f0', '#7209b7',
                '#f8961e', '#4ade80', '#f59e0b', '#10b981', '#8b5cf6',
                '#ec4899', '#06b6d4'
            ];
            
            // 更新销售额图表
            if (salesChart) {
                // 按总销售额降序排列
                selectedProducts.sort((a, b) => {
                    const sumA = a.sales.reduce((s, v) => s + v, 0);
                    const sumB = b.sales.reduce((s, v) => s + v, 0);
                    return sumB - sumA;
                });
                salesChart.data.datasets = selectedProducts.map((product, index) => ({
                    label: product.name,
                    data: product.sales,
                    backgroundColor: colors[index % colors.length] + '80', // 80表示50%透明度
                    borderColor: colors[index % colors.length],
                    borderWidth: 1,
                    borderRadius: 3
                }));
                salesChart.update();
            }
            
            // 更新同比变化图表
            if (yoyChart) {
                yoyChart.data.datasets = selectedProducts.map((product, index) => ({
                    label: product.name,
                    data: product.yoy_changes,
                    borderColor: colors[index % colors.length],
                    backgroundColor: colors[index % colors.length] + '20', // 20表示12.5%透明度
                    borderWidth: 2,
                    fill: true,
                    tension: 0.3,
                    pointRadius: 3,
                    pointHoverRadius: 5
                }));
                yoyChart.update();
            }
            
            // 更新市场份额图表
            updateMarketShareChart();
            
            updateDataTable(selectedProducts);
        }
        
        // 只更新销售额和同比变化图表
        function updateSalesAndYoyCharts(selectedProductIds) {
            const selectedProducts = productData.products.filter(p => selectedProductIds.includes(p.id));
            
            console.log('更新销售额和同比变化图表，选中' + selectedProducts.length + '个产品');
            
            // 颜色方案
            const colors = [
                '#4361ee', '#3a0ca3', '#f72585', '#4cc9f0', '#7209b7',
                '#f8961e', '#4ade80', '#f59e0b', '#10b981', '#8b5cf6',
                '#ec4899', '#06b6d4'
            ];
            
            // 更新销售额图表
            if (salesChart) {
                // 按总销售额降序排列
                selectedProducts.sort((a, b) => {
                    const sumA = a.sales.reduce((s, v) => s + v, 0);
                    const sumB = b.sales.reduce((s, v) => s + v, 0);
                    return sumB - sumA;
                });
                salesChart.data.datasets = selectedProducts.map((product, index) => ({
                    label: product.name,
                    data: product.sales,
                    backgroundColor: colors[index % colors.length] + '80', // 80表示50%透明度
                    borderColor: colors[index % colors.length],
                    borderWidth: 1,
                    borderRadius: 3
                }));
                salesChart.update();
            }
            
            // 更新同比变化图表
            if (yoyChart) {
                yoyChart.data.datasets = selectedProducts.map((product, index) => ({
                    label: product.name,
                    data: product.yoy_changes,
                    borderColor: colors[index % colors.length],
                    backgroundColor: colors[index % colors.length] + '20', // 20表示12.5%透明度
                    borderWidth: 2,
                    fill: true,
                    tension: 0.3,
                    pointRadius: 3,
                    pointHoverRadius: 5
                }));
                yoyChart.update();
            }
            
            // 更新数据表格
            updateDataTable(selectedProducts);
        }
        
        // 更新数据表格
        function updateDataTable(products) {
            const table = document.getElementById('dataTable');
            if (!table) return;
            
            const thead = table.querySelector('thead');
            const tbody = table.querySelector('tbody');
            
            // 更新表头
            let headerRow = thead.querySelector('tr');
            if (!headerRow) {
                headerRow = document.createElement('tr');
                thead.appendChild(headerRow);
            }
            
            // 清空表头（保留第一列"产品"）
            while (headerRow.children.length > 1) {
                headerRow.removeChild(headerRow.lastChild);
            }
            
            // 添加季度列
            productData.quarters.forEach(q => {
                const th = document.createElement('th');
                th.textContent = q;
                th.style.textAlign = 'center';
                headerRow.appendChild(th);
            });
            
            // 更新表格内容
            tbody.innerHTML = '';
            products.forEach(product => {
                const row = document.createElement('tr');
                
                // 产品名称列
                const nameCell = document.createElement('td');
                nameCell.textContent = product.name;
                nameCell.style.fontWeight = '500';
                row.appendChild(nameCell);
                
                // 季度数据列
                product.sales.forEach(sale => {
                    const cell = document.createElement('td');
                    cell.textContent = sale.toFixed(1);
                    cell.style.textAlign = 'right';
                    row.appendChild(cell);
                });
                
                tbody.appendChild(row);
            });
            
            console.log('数据表格更新完成');
        }
        
        // ============ FILTER UI CONSTANTS ============
        const ALL_PROVINCES = ['江苏','浙江','安徽','山东','广东','四川','湖北','湖南','河南','河北','江西','陕西','云南'];
        const ALL_CITIES = ['北京','上海','天津','重庆','广州','成都','深圳','苏州','武汉','杭州','西安','南京','无锡','温州','长沙','青岛','济南','烟台','合肥','昆明','南昌','石家庄','郑州','常州','临沂','大连','兰州','福州','宁波','厦门'];

        // ============ BUILD FILTER UI ============
        function buildFilterUI() {
            const provContainer = document.getElementById('provinceButtons');
            const cityContainer = document.getElementById('cityButtons');

            // Province buttons — single select
            provContainer.innerHTML = '';
            const allBtn = document.createElement('button');
            allBtn.className = 'filter-btn all-btn selected';
            allBtn.textContent = '全国';
            allBtn.addEventListener('click', () => onProvinceClick(null));
            provContainer.appendChild(allBtn);
            ALL_PROVINCES.forEach(p => {
                const btn = document.createElement('button');
                btn.className = 'filter-btn';
                btn.textContent = p;
                btn.addEventListener('click', () => onProvinceClick(p));
                provContainer.appendChild(btn);
            });

            // City buttons — single select
            cityContainer.innerHTML = '';
            const allCityBtn = document.createElement('button');
            allCityBtn.className = 'filter-btn all-btn';
            allCityBtn.textContent = '全部城市';
            allCityBtn.addEventListener('click', () => onCityClick(null));
            cityContainer.appendChild(allCityBtn);
            ALL_CITIES.forEach(c => {
                const btn = document.createElement('button');
                btn.className = 'filter-btn';
                btn.textContent = c;
                btn.addEventListener('click', () => onCityClick(c));
                cityContainer.appendChild(btn);
            });

            // Product buttons — multi-select
            rebuildProductFilter();
            updateFilterStyles();
            updateFilterStatus();
        }

        function rebuildProductFilter() {
            const prodContainer = document.getElementById('productButtons');
            if (!prodContainer) return;
            prodContainer.innerHTML = '';

            const allNames = productData.products.map(p => p.name);

            // 全部产品按钮
            const allProdBtn = document.createElement('button');
            allProdBtn.className = 'filter-btn all-btn' + (selectedProductNames.size === 0 ? ' selected' : '');
            allProdBtn.textContent = '全部产品';
            allProdBtn.addEventListener('click', () => onProductClick('__all__'));
            prodContainer.appendChild(allProdBtn);

            // 各产品按钮
            allNames.forEach(name => {
                const btn = document.createElement('button');
                btn.className = 'filter-btn' + (selectedProductNames.has(name) ? ' selected' : '');
                btn.textContent = name;
                btn.dataset.product = name;
                btn.addEventListener('click', () => onProductClick(name));
                prodContainer.appendChild(btn);
            });
        }

        // ============ CLICK HANDLERS ============
        function onProvinceClick(province) {
            if (province === null) {
                selectedProvinceName = '';
            } else {
                if (selectedProvinceName === province) { selectedProvinceName = ''; }
                else { selectedProvinceName = province; selectedCityName = ''; }
            }
            updateFilterStyles();
            updateFilterStatus();
            updateCharts();
        }

        function onCityClick(city) {
            if (city === null) {
                selectedCityName = '';
            } else {
                if (selectedCityName === city) { selectedCityName = ''; }
                else { selectedCityName = city; selectedProvinceName = ''; }
            }
            updateFilterStyles();
            updateFilterStatus();
            updateCharts();
        }

        function onProductClick(productName) {
            const allNames = productData.products.map(p => p.name);
            if (productName === '__all__') {
                selectedProductNames.clear();
            } else {
                if (selectedProductNames.has(productName)) {
                    selectedProductNames.delete(productName);
                } else {
                    selectedProductNames.add(productName);
                }
            }
            updateFilterStyles();
            updateFilterStatus();
            updateCharts();
        }

        // ============ BUTTON STYLES & STATUS ============
        function updateFilterStyles() {
            // Province buttons
            const provBtns = document.querySelectorAll('#provinceButtons .filter-btn');
            provBtns.forEach(btn => {
                btn.classList.remove('selected', 'disabled');
                if (selectedCityName) { btn.classList.add('disabled'); }
                else if (btn.textContent === '全国' && !selectedProvinceName) { btn.classList.add('selected'); }
                else if (btn.textContent === selectedProvinceName) { btn.classList.add('selected'); }
            });

            // City buttons
            const cityBtns = document.querySelectorAll('#cityButtons .filter-btn');
            cityBtns.forEach(btn => {
                btn.classList.remove('selected', 'disabled');
                if (selectedProvinceName) { btn.classList.add('disabled'); }
                else if (btn.textContent === '全部城市' && !selectedCityName) { btn.classList.add('selected'); }
                else if (btn.textContent === selectedCityName) { btn.classList.add('selected'); }
            });

            // Product buttons
            const prodBtns = document.querySelectorAll('#productButtons .filter-btn');
            prodBtns.forEach(btn => {
                btn.classList.remove('selected');
                if (btn.textContent === '全部产品' && selectedProductNames.size === 0) { btn.classList.add('selected'); }
                else if (selectedProductNames.has(btn.textContent)) { btn.classList.add('selected'); }
            });
        }

        function updateFilterStatus() {
            const el = document.getElementById('currentFilterStatus');
            if (!el) return;
            let parts = [];
            if (selectedProvinceName) parts.push(selectedProvinceName);
            else if (selectedCityName) parts.push(selectedCityName);
            else parts.push('全国');
            if (selectedProductNames.size > 0) parts.push('产品: ' + [...selectedProductNames].join(', '));
            else parts.push('全部产品');
            el.textContent = parts.join(' | ');
        }

        // ============ INIT ============
        function init() {
            initSalesChart();
            initYoyChart();
            initMarketShareChart();
            buildFilterUI();
            updateCharts();
        }
        document.addEventListener('DOMContentLoaded', init);
    </script>
</body>
</html>